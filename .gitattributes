# When shell scripts end in CRLF, bash gives a cryptic error message
*.sh text eol=lf
# Encrypt sbin
sbin/*.sh eol=lf filter=gpg
# git config filter.gpg.clean 'gpg -q -e -r git -a'
# git config filter.gpg.smudge 'gpg -d -q'
