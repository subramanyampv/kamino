// {{ fileName }}
{{#each imports}}
import { {{ typeName }} } from "./{{ fileName }}";
{{/each}}
{{#if imports}}

{{/if}}
export interface {{ typeName }}
{{~#if baseTypes}} extends {{#each baseTypes ~}}
{{ typeName }}{{#unless @last}}, {{/unless}}
{{~/each}}
{{~/if}} {
  {{#each properties}}
  {{ name }}{{#unless required}}?{{/unless}}: {{ type }};
  {{/each}}
}
