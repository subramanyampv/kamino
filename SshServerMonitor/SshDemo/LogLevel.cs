namespace SshDemo
{
    /// <summary>
    /// Log levels to monitor.
    /// The names are converted to uppercase and are searched for inside log files.
    /// </summary>
    enum LogLevel
    {
        Debug = 0,
        Info = 1,
        Warn = 2,
        Error = 3
    }
}