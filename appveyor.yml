version: "{build}"

build: off

clone_folder: c:\gopath\src\github.com\ngeor\yart

environment:
  GOPATH: c:\gopath

stack: go 1.10

before_test:
- go vet ./...

test_script:
- go test -v ./...
- go build

artifacts:
- path: yart.exe
  name: yart.exe
